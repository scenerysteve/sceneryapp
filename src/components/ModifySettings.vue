<template>
  <!-- TODO add form markup here, will probably need to use slots for the status inputs -->
</template>

<script>
import Vue from "vue";

export default Vue.extend({
  name: "ModifySettings",
  props: ["settings"]
});
</script>
